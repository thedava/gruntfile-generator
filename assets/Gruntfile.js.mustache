'use strict';

//
// This file is @AUTOGENERATED
//

module.exports = function(grunt) {
    grunt.initConfig({{target_config}});

    // imports{{#imports}}
    grunt.loadNpmTasks('{{.}}');{{/imports}}

    // tasks{{#tasks}}
    grunt.registerTask('{{task_name}}', {{task_commands}});{{/tasks}}
};
